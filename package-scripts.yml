scripts:
  start:
    script: vite
    description: 🚀 Запуск Vite dev-сервера

  dev:
    script: storybook dev -p 6006
    description: 📘 Запуск Storybook на порту 6006

  prebuild:
    script: rm -rf dist
    description: 🧨 Очистка папки dist перед сборкой

  generate-index:
    script: node lib/scripts/generate-index.js
    description: 📄 Генерация общего index-файла для компонентов

  build:
    script: 'pnpm run generate-index && vite build && tsc'
    description: 📦 Сборка проекта (index, vite, tsc)

  preview:
    script: vite preview
    description: 👀 Просмотр собранного проекта

  lint:
    script: 'eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0'
    description: 🔍 Линтинг проекта

  lintFix:
    script: 'eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0 --fix'
    description: 🧹 Линтинг с авто-фиксом

  buildStorybook:
    script: storybook build
    description: 📘 Сборка Storybook

  addChangeset:
    script: 'git add . && read -p "Введите комментарий к коммиту: " msg && git commit -m "$msg"'
    description: 📦 Добавление изменений с ручным комментарием

  version:
    patch:
      script: 'pnpm version patch && git commit -m "chore: bump patch version" && git push git@github.com:SemAntony/design-system.git HEAD && git push git@github.com:SemAntony/design-system.git --tags'
      description: 🔖 Повышение patch-версии

    minor:
      script: 'pnpm version minor && git commit -m "chore: bump minor version" && git push git@github.com:SemAntony/design-system.git HEAD && git push git@github.com:SemAntony/design-system.git --tags'
      description: 🆙 Повышение minor-версии

    major:
      script: 'pnpm version major && git commit -m "chore: bump major version" && git push git@github.com:SemAntony/design-system.git HEAD && git push git@github.com:SemAntony/design-system.git --tags'
      description: 🚀 Повышение major-версии

    beta:
      script: 'pnpm version prerelease --preid=beta && git commit -m "chore: bump beta version" && git push git@github.com:SemAntony/design-system.git HEAD && git push git@github.com:SemAntony/design-system.git --tags'
      description: 🧪 Выпуск beta-версии

    alpha:
      script: 'pnpm version prerelease --preid=alpha && git commit -m "chore: bump alpha version" && git push git@github.com:SemAntony/design-system.git HEAD && git push git@github.com:SemAntony/design-system.git --tags'
      description: 🧬 Выпуск alpha-версии

    rc:
      script: 'pnpm version prerelease --preid=rc && git commit -m "chore: bump rc version" && git push git@github.com:SemAntony/design-system.git HEAD && git push git@github.com:SemAntony/design-system.git --tags'
      description: 🏁 Выпуск release candidate

    preminorBeta:
      script: 'pnpm version preminor --preid=beta && git commit -m "chore: bump preminor beta version" && git push git@github.com:SemAntony/design-system.git HEAD && git push git@github.com:SemAntony/design-system.git --tags'
      description: ⚗️ Выпуск preminor beta-версии
